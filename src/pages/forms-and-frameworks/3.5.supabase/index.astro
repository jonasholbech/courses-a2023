---
import SlideLayout from "../../../layouts/SlideLayout.astro";
import codeFetcher from "../../../js/codeFetcher";
import Code from "../../../components/Code.astro";
//import Explainer from "../../../components/Explainer.svelte";
import AgendaSection from "../../../components/AgendaSection.astro";
import Goal from "../../../components/Goal.astro";
const snippets = await codeFetcher("forms-and-frameworks/3.supabase", []);
---

<SlideLayout title="supabase">
  <AgendaSection header="more supabase" />
  <section>
    <p>Things I learned over the night</p>
    <p class="fragment">
      Actually, I've learned it before, but forgotten, now it's documented'
    </p>
  </section>

  <section>
    <p>
      Our <code>DEL</code>, <code>PATCH</code> &amp; <code>POST</code> requests return
      nothing
    </p>
    <p>If we add a <code>header</code> with</p>
    <p><code>Prefer: return=representation</code></p>
    <p>It does</p>
    <p class="fragment">As an array</p>
  </section>
</SlideLayout>
